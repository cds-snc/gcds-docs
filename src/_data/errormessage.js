module.exports = {
  en : {
    heading: "Keep hint text static and perceivable",
    listitems: {
      0: "Use the required attribute for fields that must be filled in. This will place “required” at the end of the field label. Avoid adding red text and additional symbols or punctuation, like the asterisk (*).",
      1: "Use the error message component to address missing and invalid entries for all required inputs. Avoid using them for optional ones.",
      2: "Place an inline error message just before the input (either to its immediate left or above it) so it's noticeable.",
      3: "Avoid placing hint text or error messages below the container where they might be hidden.",
    }
  },
  fr: {
    heading: "Keep hint text static and perceivable",
    listitems: {
      0: "Use the required attribute for fields that must be filled in. This will place “required” at the end of the field label. Avoid adding red text and additional symbols or punctuation, like the asterisk (*).",
      1: "Use the error message component to address missing and invalid entries for all required inputs. Avoid using them for optional ones.",
      2: "Place an inline error message just before the input (either to its immediate left or above it) so it's noticeable.",
      3: "Avoid placing hint text or error messages below the container where they might be hidden.",
    }
  }
}